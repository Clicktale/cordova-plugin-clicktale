def sdkVersion = '3.0.+'
def sdkRepo = 'release'

repositories {
    maven {
           url "https://androidsdk.clicktale.com/artifactory/$sdkRepo"
    }

}

dependencies {
    compile "com.clicktale.library:sdk:$sdkVersion"
}